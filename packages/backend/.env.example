# RigBoss Backend (.env)
# Copy this file to `.env` on your server backend and adjust values.

# Server Configuration
PORT=3001
HOST=0.0.0.0

# Radio Configuration
RIGCTLD_HOST=127.0.0.1
RIGCTLD_PORT=4532

# Audio Configuration
AUDIO_ENABLED=true
AUDIO_SAMPLE_RATE=48000
AUDIO_BUFFER_SIZE=1024

# Spectrum/Analyzer Configuration
# ALSA device for PCM capture (Linux). Examples: default, hw:1,0, hw:3,0
# Use arecord -l to list devices; map card N, device M to hw:N,M
SPECTRUM_DEVICE=default
# On macOS (dev), avfoundation default is used automatically.

# CORS Configuration
CORS_ORIGIN=http://localhost:4321,http://10.0.0.0/24
FRONTEND_URL=http://localhost:4321

# Logging
LOG_LEVEL=info
NODE_ENV=production