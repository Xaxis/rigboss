# RigBoss Backend (.env)
# Copy this file to `.env` on the Raspberry Pi and adjust values.

# HTTP server
BACKEND_PORT=3001
NODE_ENV=production
LOG_LEVEL=info

# Radio adapter
USE_REAL_RADIO=true
RIGCTLD_HOST=127.0.0.1
RIGCTLD_PORT=4532

# CORS allowlist for HTTP + WebSocket
# Comma-separated list of allowed origins (no spaces). Example values:
#  - http://localhost:4321                (frontend running on your laptop)
#  - http://10.0.0.29:4321                (replace with your laptop's IP)
# In production, do NOT use "*". Provide explicit origins.
CORS_ORIGIN=http://localhost:4321,http://10.0.0.29:4321

